services:
  - type: web
    name: webpro2026
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: webpro_db
          property: connectionString
